[package]
name = "docker-volume-cleaner"
version = "0.1.0"
edition = "2021"

[dependencies]
dotenv = "0.15.0"
#ssh2 = "0.3.3" # fixme cette lib est plus populaire mais ne fonctionne pas sous windows.
ssh-rs = "0.5.0"
